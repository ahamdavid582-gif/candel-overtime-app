// JSpreadsheet removed — replaced by `SpreadsheetGrid.jsx` (react-data-grid).
// This stub preserves the module export so other imports won't fail.
import React from 'react';
export default function JSpreadsheet() {
  return (
    <div style={{ padding: 12, background: '#fff', borderRadius: 8 }}>
      <strong>Legacy spreadsheet removed.</strong>
      <div>Use the updated Master Sheet (Spreadsheet) in the Admin Dashboard.</div>
    </div>
  );
}
